{
  "template": "jekyll",
  "ports": [4000],
  "name": "jekyll-for-librarians",
  "description": "jekyll template for galleries and student work",
  "scripts": {
    "post-create": "bundle install",
    "Start jekyll-for-librarians": "cd ~/code/jekyll-for-librarians && jekyll serve --host 0.0.0.0 --port 4000"
  }
}
